<%= turbo_stream_from 'products' %>
<div id='viewport'>
  <!-- Content -->
  <div id='content'>
    <div class='container-fluid'>
      <div id="products" class="row products">
        <div class='row g-5 justify-content-center align-items-center vh-100 flex-wrap'>
          <% @products.each do |product| %>
            <%= render partial: 'products/products', locals: { product: product } %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
